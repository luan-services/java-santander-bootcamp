# Exceptions

Basic Java examples covering checked exceptions, unchecked exceptions, custom exceptions, `try`, `catch`, `finally`, `throw`, and `throws`.

Source files are located in the `src` folder.
